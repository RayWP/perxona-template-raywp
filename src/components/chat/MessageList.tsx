type Message = { role: "user" | "assistant"; content: string };
export function MessageList({ messages }: { messages: Message[] }) { return <div className="flex min-h-64 flex-1 flex-col gap-3 overflow-y-auto pr-1">{messages.length === 0 && <p className="text-sm text-slate-500">Ask a generic question to test the LLM and avatar flow.</p>}{messages.map((message, index) => <div key={`${message.role}-${index}`} className={`max-w-[90%] rounded-xl px-4 py-3 text-sm ${message.role === "user" ? "self-end bg-ink text-white" : "bg-slate-100 text-ink"}`}><span className="mb-1 block text-[10px] uppercase tracking-widest opacity-60">{message.role}</span>{message.content}</div>)}</div>; }
